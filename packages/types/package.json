{
  "name": "@tessera/types",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"no tests in @tessera/types\"",
    "lint": "echo \"lint: @tessera/types\""
  },
  "devDependencies": {
    "typescript": "catalog:"
  }
}
